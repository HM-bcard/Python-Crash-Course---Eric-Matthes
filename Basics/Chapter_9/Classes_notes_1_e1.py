class Dog:
    """A docstring because it needs one"""

    def __init__(self,name,age):#so an init will fill in automatically 
        """define the init function properties"""
        self.name = name
        self.age = age # these two i don't understand
        